# 이 레포에서 일하는 법

**검시 스킬(도구) 자체를 관리하는 공개 레포다.** coroner / executor / design-coroner
세 스킬과, 산출물의 근거 주장을 실제 도구 호출 기록과 대조하는 rigor Stop 훅이 들어 있다.

**아이디어 판정 작업은 여기서 하지 않는다.** 판정문·타인 아이디어 원문·주제 선정은
`~/orca/projects/ai-championship-2026/` (비공개, 원격 없음). 헷갈리면 이렇게 나눈다 —
**판정 내용은 저기, 판정이 어떻게 나왔는가는 여기.**

여기 남는 것: 부재단정 여부, 훅 반송이 정탐/오탐인지, 어휘 2계열 확대와 원문 확인 여부,
이해충돌 공시 여부. → `docs/run-ledger.md`

## 구조

| 경로 | 내용 |
|---|---|
| `plugin/hooks/rigor.py` | Stop 훅 본체 |
| `plugin/hooks/run-rigor.sh` | 런처. 인터프리터를 **이름이 아니라 동작으로** 고른다 |
| `plugin/hooks/test_rigor.py` | 회귀 스위트 41케이스 |
| `plugin/hooks/rigor-spec.md` | 훅 사양 + 실측 기록 + 설계 원칙 |
| `skills/` · `plugin/skills/` | **동일 내용이 이중 트래킹된다** (아래 주의) |
| `docs/run-ledger.md` | 실행 원장 — 런별 관찰 |
| `docs/failure-log.md` | 실패 기록. 이 레포의 모든 변경 근거 |
| `docs/experiment-report.md` | 티어 실험·Δ 실험 리포트 |

## 개발 규율

이 레포의 원칙은 하나다 — **"Failure가 Requirement보다 먼저."**
모든 변경은 관찰된 실패에서 나온다. 추측으로 규칙을 만들지 않는다.

1. **실런 문장을 회귀 케이스로 고정한다.** 정규식으로 산문을 읽는 이상 "언급 vs 주장"은
   원리적으로 안 닫힌다. 일반 해법을 시도하지 말고 실제 문장을 케이스로 쌓는다.
2. **표본 0개로 규칙을 세우지 않는다.** "백틱=논의" 규칙이 실런 5개에 반증된 전례가 있다.
3. **정직을 벌하지 않는다.** 손실 함수가 비대칭이다 — **위조 1건 미탐보다 정직 1건 오탐이
   나쁘다.** 오탐은 모델에게 공시를 그만두라고 가르치는 훈련 신호다.
   상세: `plugin/hooks/rigor-spec.md`의 「설계 원칙」 절.
4. **감시 범위를 산출물 마커 밖으로 넓히지 않는다.** 오탐 표면적이 곧 정직 처벌 표면적이다.
5. **측정하지 않은 것을 수치로 쓰지 않는다.** 수치를 쓸 때는 유효 범위를 같이 적는다.

## 작업 절차

```bash
python plugin/hooks/test_rigor.py     # 41/41 이어야 한다
claude plugin validate ./plugin
diff -rq skills plugin/skills          # 이중 트래킹 동기 확인
```

**주의 — 스킬 문서를 고칠 때는 6파일이다.** `skills/`와 `plugin/skills/`에 동일 내용이
각각 트래킹된다. 한쪽만 고치면 배포본과 어긋난다. 정리 여부는 미결(별도 작업).

**주의 — 훅을 고치면 `plugin.json`의 `version`을 올려야 한다.** 설치본은 버전 문자열로
캐시되는 복사본이므로, 안 올리면 `claude plugin update`가 "already at the latest version"
이라며 **옛 복사본을 조용히 유지한다**(실측). 개발 중에는 `claude --plugin-dir ./plugin`
(항상 라이브)을 쓴다.

## 무증상 실패 계열 — 이 레포의 최대 위험

게이트가 무력해질 때 **증상은 오류가 아니라 '통과'다.** 오탐은 시끄럽게 드러나지만
미실행은 조용하다. 2026-08-31에 네 개를 잡았고(`docs/failure-log.md` 26~29-b),
넷 다 증상이 통과였다.

- 셔뱅 `python3`가 Microsoft Store 스텁으로 잡혀 exit 0 → Windows에서 훅 미실행
- stdin이 로케일(cp949)로 열려 한글이 깨지고 전부 `skip`
- 버전 미범프로 설치본이 옛 복사본 유지
- **거부된 도구 호출이 근거로 계산** → 호출만 던지고 태그 붙이면 뚫림

**조기 경보는 하나뿐이다** — `RIGOR_LOG=<파일>` 을 켜두고 줄이 쌓이는지 본다.
한 줄도 없으면 미실행이다. 새 환경에서 처음 돌릴 때는 반드시 확인한다.

## 열린 항목 (2026-09-01)

- 순수 자연 조건에서 배너 회차 도달 표본 — 0건
- 오탐률 재측정 — 기존 0/114는 도구 호출 기록이 없어 **사후 검증 불가 확정**.
  재측정 시 결과까지 기록에 남기는 절차로
- "세션 도구 목록을 봤다 → `[FILE]`" 오용이 Opus·Sonnet 양쪽에서 3회 재발 (미해소)
- `skills/`·`plugin/skills/` 이중 트래킹 정리
- 블로그 3부작 — 3부에서 레포 공개 예정. 그 전까지 외부 공개 확대 금지

`HANDOFF.md`(gitignore)에 설계 대화로 되돌릴 질문이 정리돼 있다.
